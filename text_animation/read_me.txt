1) The translate() method moves an element from its
   current position (according to the parameters given for the X-axis and the Y-axis).
  -50% from its current position to left or to up

2)ease-in-out	means that The animation has both a slow start and a slow end

3)The -webkit-text-stroke CSS property specifies the width and color
  of strokes for text characters. 
